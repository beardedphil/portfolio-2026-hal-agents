# Verification (no external tools)

1. In the file explorer, open `.cursor/rules/`.
2. Confirm the only file present is `SUPERREPO_RULES_ONLY.mdc`.
3. Open `SUPERREPO_RULES_ONLY.mdc` and confirm it instructs opening `portfolio-2026-hal/` for authoritative rules.

