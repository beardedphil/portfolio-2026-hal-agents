# Changed files

- `src/agents/projectManager.ts` (updated: removed `stepCountIs` import; replaced `stopWhen: stepCountIs(MAX_TOOL_ITERATIONS)` with `maxSteps: MAX_TOOL_ITERATIONS`)
- `docs/audit/0004-fix-pm-agent-stepcountis-and-sync-hal-submodule/*` (added: plan, worklog, changed-files, decisions, verification, pm-review)
