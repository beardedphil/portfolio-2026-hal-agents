# Tickets

Ticket files live in this folder as Markdown (`0001-something.md`, etc.).

- These files are the **canonical specs** for work.
- In HAL/Kanban, tickets are synced into Supabase using `npm run sync-tickets` (see repo root).
